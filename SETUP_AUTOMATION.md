# 自動メール配信のセットアップ

毎日と毎週、レポートがメールで届くようにします。
**一度設定すれば、以降の操作は不要**です。PCの電源が入っていなくても動きます。

所要時間：**30〜40分**（一度きり）

---

## 先に把握しておいてほしいこと

### ① Freeプランのままだと、日次メールは売買判断に使えません

Freeプランはデータが12週間遅延します。日次メールには「3か月前の銘柄」が載ります。
メール本文には遅延日数の警告が自動で入りますが、内容そのものは古いままです。

| 目的 | 必要なプラン |
|---|---|
| 週次の検証レポートだけ受け取る | **Free（0円）で十分** |
| 日次で実際に使える候補を受け取る | **Light（1,650円/月）が必要** |

まずはFreeで週次だけ動かし、レポートの中身を見てからLightに切り替える、で構いません。

### ② LLM（生成AI）は使っていません

「想定を入れない」ため、文章を生成するAIをこの仕組みから完全に外しました。

**LLMのハルシネーションをゼロにする方法はありません。** プロンプトで抑制はできますが、
保証はできません。購入判断に直結する以上、使わないのが唯一の答えです。

代わりに、Role 6（反対材料の洗い出し）を**決定論的なチェックリスト**に置き換えています。

| チェック項目 | 何を見るか | 出典 |
|---|---|---|
| 経営者の予想達成率 | 会社予想 vs 実績の履歴 | J-Quants 決算サマリー |
| 希薄化 | 発行済株式数が1年で10%以上増えたか | J-Quants 決算サマリー |
| 保有期間中の決算 | 3か月以内に決算発表があるか | J-Quants 決算発表日 |
| 直近の急騰 | 60営業日で既に2倍以上か | J-Quants 株価日足 |
| 需給先行 | 出来高だけ先行し業績の裏付けが薄くないか | 株価＋決算 |
| PSR | 時価総額 ÷ 年換算売上が10倍以上か | J-Quants 決算サマリー |

すべて **APIから取得した数値と、その計算結果のみ**です。文章生成はありません。
各判定には出典が併記され、どの数値から出したかを追えます。

**最も重要な原則：「不明」は不合格として扱います。**
データが取れなかった項目は埋めません。通しません。
不明を中立扱いにすると、データが欠けているだけの銘柄が通過してしまうためです。

### ③ 「全項目通過」は買い推奨ではありません

チェックを通過したという事実の記述です。**最終的な購入判断はご自身で行ってください。**

そして正直に申し上げると、**ハルシネーションが無いことと、判断が正しいことは別**です。
データの誤り、閾値の設定ミス、上場廃止銘柄の扱いなど、決定論的な仕組みでも
間違いは起こります。この仕組みが保証するのは「推測を混ぜていないこと」だけです。

---

## セットアップ

### ステップ1：Gmailのアプリパスワードを発行（10分）

通常のGmailパスワードではSMTP送信できません。専用のパスワードが要ります。

1. Googleアカウントで**2段階認証を有効化**（未設定の場合）
   - https://myaccount.google.com/security
2. 同じセキュリティ画面から「アプリ パスワード」を開く
3. 名前を「株スクリーン」などにして作成
4. **16桁のパスワードが表示される** → これをコピー（再表示できません）

> このパスワードはGmail送信専用で、アカウント本体のパスワードとは別物です。
> 不要になったらいつでも取り消せます。

### ステップ2：GitHubにリポジトリを作る（10分）

1. https://github.com/ でアカウント作成（無料）
2. 右上「+」→「New repository」
3. 名前：`stock-screener`
4. **必ず Private を選択**
5. 「Create repository」

### ステップ3：ファイルをアップロード（5分）

作成されたリポジトリ画面で「uploading an existing file」をクリックし、
ZIPを展開したファイルを**すべてドラッグ&ドロップ**します。

⚠️ `.github` フォルダも忘れずに含めてください。これが無いと自動実行されません。
（隠しフォルダ扱いになる環境では、表示設定で見えるようにしてください）

### ステップ4：APIキーとメール設定を登録（10分）

リポジトリの **Settings** → 左メニュー **Secrets and variables** → **Actions**
→ 「New repository secret」で、以下を1つずつ登録します。

| Name | Secret（値） |
|---|---|
| `JQUANTS_API_KEY` | J-QuantsのAPIキー |
| `MAIL_FROM` | 送信元のGmailアドレス |
| `MAIL_PASSWORD` | ステップ1の16桁アプリパスワード |
| `MAIL_TO` | 受信したいアドレス（自分宛でOK） |


> Secretsに入れた値は、登録後は本人にも表示されません。
> **コードに直接書かないでください。** リポジトリがPrivateでも、
> キーが平文で残るのは避けるべきです。

### ステップ5：動作確認（5分）

1. リポジトリの **Actions** タブを開く
2. ワークフローの有効化を促されたら承認
3. 左メニューから「週次レポート」を選択
4. 右側の **「Run workflow」** ボタン → 実行

数分でメールが届きます。届かない場合は Actions の実行ログに原因が出ます。
**エラー時もメールが飛ぶ設定**にしてあるので、そのメールをチャットに貼ってください。

---

## 配信スケジュール

| | タイミング | 内容 |
|---|---|---|
| **日次** | 平日 17:30 JST | 相場環境の判定＋当日の抽出銘柄 |
| **週次** | 土曜 08:00 JST | 今週の候補＋**過去の抽出銘柄がその後どうなったか** |

時刻を変えたい場合は `.github/workflows/*.yml` の `cron` を編集してください（UTC表記です）。

日次メールが不要なら、`.github/workflows/daily.yml` を削除すれば週次だけになります。

---

## 週次メールで一番見るべき場所

「これまでの抽出銘柄の追跡」の**中央値**です。

```
対象 47銘柄 / 中央値 +2.3% / プラスの銘柄 26件 / 3倍達成 0件
```

ここが数か月マイナスで推移するなら、スクリーニング条件が機能していません。
`config.py` の数値を**一度に1つだけ**変更して、変化を見てください。

抽出履歴はリポジトリの `history/` フォルダに自動で蓄積されます。
これが検証の材料になるので、消さないでください。

---

## 運用が壊れていないかの確認

自動化で最も危険なのは、**動いていないことに気づかないまま数週間経つ**ことです。

- 平日は毎日メールが届くはず（候補ゼロでも届きます）
- **2日以上届かなければ異常**です。Actionsタブの実行履歴を確認してください
- GitHubは長期間コミットが無いとスケジュール実行を停止することがあります。
  日次ジョブが履歴をコミットするので通常は起きませんが、頭に入れておいてください

---

## 費用

| | 月額 |
|---|---|
| GitHub Actions（Privateリポジトリ） | 無料枠内（この処理量なら十分収まります） |
| Gmail送信 | 0円 |
| J-Quants Free | 0円 |
| J-Quants Light（日次を実用にする場合） | 1,650円 |
